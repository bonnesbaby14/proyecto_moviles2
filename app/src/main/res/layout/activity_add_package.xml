<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/background"
    tools:context=".AddPackage">

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_baseline_fingerprint_24"
        android:layout_marginVertical="20dp"

        android:id="@+id/imageTomar"
        ></ImageButton>

    <EditText
        android:id="@+id/code"
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:text="Codigo paquete: "

        android:textColor="#FFF"
        android:textSize="25dp" />
    <EditText
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:text="Descripcion paquete: "
        android:textSize="25dp"
        android:textColor="#FFF"

        android:layout_marginTop="40dp"
        android:id="@+id/des"
        />
    <EditText
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:text="Cordenadas entrega: "
        android:textSize="25dp"
        android:textColor="#FFF"
        android:layout_marginTop="40dp"
        android:id="@+id/cordenadas"
        />
    <Button
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:text="Obtener cordenadas"
        android:textSize="15dp"
        android:textColor="#FFF"
        android:layout_marginTop="30dp"
        android:id="@+id/getcord"
        />

    <Button
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:text="Enviar"
        android:textSize="15dp"
        android:textColor="#FFF"
        android:layout_marginTop="45dp"
        android:id="@+id/enviar"
        />

</LinearLayout>